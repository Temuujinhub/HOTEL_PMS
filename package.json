{
  "name": "cloud-pms",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud PMS — Multi-tenant hotel & property management SaaS",
  "license": "UNLICENSED",
  "scripts": {
    "dev:api": "npm --prefix apps/api run start:dev",
    "dev:web": "npm --prefix apps/web run dev",
    "build": "npm run build:api && npm run build:web",
    "build:api": "npm --prefix apps/api run build",
    "build:web": "npm --prefix apps/web run build",
    "install:all": "npm --prefix apps/api install && npm --prefix apps/web install",
    "lint": "npm --prefix apps/api run lint && npm --prefix apps/web run lint",
    "db:migrate": "npm --prefix apps/api run prisma:migrate",
    "db:seed": "npm --prefix apps/api run prisma:seed",
    "up": "docker compose up -d --build",
    "down": "docker compose down"
  },
  "engines": {
    "node": ">=20"
  }
}
